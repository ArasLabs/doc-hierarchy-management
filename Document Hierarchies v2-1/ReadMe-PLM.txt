Added Properties to ItemType "Document"
-published_file_type = type item linking to "FileType"
-published_file_type_icon = foreign
-set label of 'is_current' to '.' so it shows on document hierarchy's grid headings
-set label of 'generation' to 'Gen' so it shows on document hierarchy's grid headings